var config = {};

config.server = {
	port: 3000,
	exposedUrl: "http://127.0.0.1:3000" // No trailing slash ! Place here the exposed URL of this server. This will be used by clients 
};
module.exports = config;